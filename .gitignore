# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# staff portal sqlite database
/data

# Transatel SIM delivery files — contain real ICCID/PIN/PUK for physical
# stock, never commit these. Run `npm run seed:sims -- <path>` locally to
# import a delivery file into the database instead.
scripts/source/sim-inventory*.csv

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
